{
  "name": "loop-im-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:deployed": "playwright test --config playwright.deployed.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
